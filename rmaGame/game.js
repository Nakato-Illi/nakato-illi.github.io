window.onload = function () {

    var config = {
        width: 800,
        height: 600,
        backgroundColor: 0x345345,
        scene: [Scene1, Scene2]
    }

    var game = new Phaser.Game(config);
}